schema_version: 1
name: engine_cleaning_aligned
scene_type: engine_cleaning

metadata:
  generated_from: configs/scenes/engine_cleaning.yaml
  alignment_mode: grounded
  note: Derived from mesh bbox diagnostics; verify regions and nozzle alignment in viewer.

engine:
  assets:
    visual_mesh: ../../assets/engine/meshes/engine_visual.stl
    collision_mesh: ../../assets/engine/collision/engine_collision.stl
  scale: 0.001
  pose:
    position_m: [-1.12127, -1.32342, -3.48744]
    quat_wxyz: [1.0, 0.0, 0.0, 0.0]

primitive_collision_geoms:
  - name: nozzle_collision_hint
    type: capsule
    enabled: false
    position_m: [0.0, 0.0, 0.4]
    quat_wxyz: [1.0, 0.0, 0.0, 0.0]
    radius_m: 0.03
    length_m: 0.4
    note: Placeholder only. Needs manual alignment to the nozzle.

regions:
  entry_port:
    type: circular_port
    center_m: [0.28, 0.0, 0.10]
    normal: [-1.0, 0.0, 0.0]
    radius_m: 0.045
    description: Main insertion entry for future observer and executor arms.

  inspection_roi:
    type: roi_box
    center_m: [0.36, 0.0, 0.12]
    size_m: [0.12, 0.10, 0.08]
    description: Coarse inspection volume for first-pass engine observation.

  carbon_deposit_region:
    type: surface_patch
    position_m: [0.40, 0.015, 0.095]
    normal: [-1.0, 0.0, 0.0]
    extents_m: [0.05, 0.03, 0.01]
    description: Placeholder region for future deposit localization and path generation.

  forbidden_zone:
    type: box
    center_m: [0.42, 0.0, 0.18]
    size_m: [0.08, 0.06, 0.04]
    description: Keep-out region reserved for no-go geometry and future collision policies.
